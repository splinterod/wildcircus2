{% extends 'base.html.twig' %}

{% block title %}Artistes{% endblock %}

{% block body %}
    <h1>Artistes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ artiste.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ artiste.name }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ artiste.firstname }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ artiste.lastname }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ artiste.mail }}</td>
            </tr>
            <tr>
                <th>Style</th>
                <td>{{ artiste.style }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ artiste.price }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ artiste.picture }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ artiste.password }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('artistes_index') }}">back to list</a>

    <a href="{{ path('artistes_edit', {'id': artiste.id}) }}">edit</a>

    {{ include('artistes/_delete_form.html.twig') }}
{% endblock %}
